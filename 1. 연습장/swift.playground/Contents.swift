import UIKit

//<상수 및 변수 선언>
//let 상수키워드 var 변수키워드
//변수를 여러개 선언하고싶을경우 var x = 0.0, y = 0.0, z = 0.0 (쉼표로 구분)
let hii = 10
var hi = 0

//<Type 선언>
//(문자변수를 저장할때)
var welcomeMessage : String //welcomeMessage 는 문자형타입선언 그안에 변수저장 가능
welcomeMessage = "hello" //변수에 문자내용 입력
print(welcomeMessage)  //welcomeMessage에 저장된 변수가 출력됨

//(Int형 변수 저장)
var welcome : Int
welcome = 123
print(welcome)

